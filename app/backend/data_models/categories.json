{
    "title": "categories",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "name": {
            "type": "string",
            "description": "Category slug/key, e.g. technology"
        },
        "label": {
            "type": "string",
            "description": "Display label, e.g. Technology"
        },
        "created_at": {
            "type": "string",
            "format": "date-time",
            "description": "Creation timestamp"
        }
    },
    "required": [
        "id",
        "name",
        "label"
    ]
}